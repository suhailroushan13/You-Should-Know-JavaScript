var priceList = [1, 2, 3, 4, 5, 6, 7, 8, 9];

function find(arr, num) {
  let news;
  for (let i = 0; i <= arr.length - 1; i++) {
    news = new Array(arr).flat();

    console.log(news[i]);

 
   

  }
}
find(priceList, 10);
